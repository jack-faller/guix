[user]
	name = Jack Faller
	email = jack.t.faller@gmail.com
	signingkey = D97A5464A39203661ED95C07A0437B429C104C61
[commit]
	gpgsign = true
[http]
	postBuffer = 15728640
[credential]
	helper = store
[pull]
	ff = only
[mergetool]
	keepBackup = true
[gc]
	auto = 1
[init]
	defaultObjectFormat = sha256
[sendemail]
	# Enable 2FA on Google account.
	# Generate a passcode at https://myaccount.google.com/apppasswords.
	# Specify it once to `git send-email` using the `--smtp-pass` switch, and it will be stored in the credentials file.
	smtpserver = smtp.gmail.com
	smtpuser = jack.t.faller@gmail.com
	smtpencryption = ssl
	smtpserverport = 465
